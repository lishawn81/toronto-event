name: Update Toronto Events

on:
  schedule:
    - cron: '0 9 * * *'   # 5:00 AM Toronto time (09:00 UTC)
    - cron: '0 21 * * *'  # 5:00 PM Toronto time (21:00 UTC)
  workflow_dispatch:      # Allows manual trigger

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: github-pages

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install dependencies
        run: pip install -r requirements.txt

      - name: 📝 Run event scraper
        run: python events.py

      - name: 🔄 Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/index.md
          git commit -m "📅 Update events (automated)" || echo "No changes to commit"
          git push
